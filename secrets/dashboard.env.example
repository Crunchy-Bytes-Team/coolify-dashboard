# Replace with the gateway password generated in your Coolify resource.
# Add one distinct variable per gateway and reference its name in servers.json.
METRICS_GATEWAY_TOKEN=replace-with-your-gateway-token
